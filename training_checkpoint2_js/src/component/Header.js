function Header () {
    const name = "Welcome to React to the games";

    return (
        <div>
            <h1>
                {name}
            </h1>
        </div>
    )
}

export default Header;